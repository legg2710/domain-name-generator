  let pronoun = ['the','our'];
  let adj = ['great', 'big' ];
  let noun = ['jogger','racoon',];

  let randomain = Math.floor(Math.random() * 2);

  let domain = pronoun[randomain] + adj[randomain] + noun[randomain] + ".com"

  console.log (domain)
  